@model ProjetAppli_v1.Models.parametrageHabilitations

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>parametrageHabilitations</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.identifiant)

        @Html.HiddenFor(model => model.debutValidite)

        @Html.HiddenFor(model => model.finValidite)

        @Html.HiddenFor(model => model.nom)

        @Html.HiddenFor(model => model.prenom)

        @Html.HiddenFor(model => model.email)

        @Html.HiddenFor(model => model.motDePasse)

        @Html.HiddenFor(model => model.role)

        @Html.HiddenFor(model => model.domaines)

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
